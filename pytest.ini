[pytest]
minversion = 7.0
pythonpath = .
# Only discover tests in the tests directory, ignore __pypackages__ and other dirs
testpaths = tests
# Exclude patterns to avoid scanning dependency directories  
norecursedirs = __pypackages__ .venv .git .tox dist build *.egg
