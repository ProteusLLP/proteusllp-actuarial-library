diff --git a/src/pal/stochastic_scalar.pyi b/src/pal/stochastic_scalar.pyi
--- a/src/pal/stochastic_scalar.pyi
+++ b/src/pal/stochastic_scalar.pyi
@@ -5,6 +5,7 @@
 import typing as t
 
 import numpy as np
 import numpy.typing as npt
+import plotly.graph_objects as go
 
 from ._compat import Self
 from .couplings import CouplingGroup, ProteusStochasticVariable
@@ -178,8 +179,10 @@
     @t.overload
     def tvar(self, p: list[Numeric]) -> list[float]: ...
     def upsample(self, n_sims: int) -> Self: ...
-    def show_histogram(self, title: str | None = None) -> None: ...
-    def show_cdf(self, title: str | None = None) -> None: ...
+    def histogram(self, title: str | None = None) -> go.Figure: ...
+    def show_histogram(self, title: str | None = None) -> go.Figure: ...
+    def cdf(self, title: str | None = None) -> go.Figure: ...
+    def show_cdf(self, title: str | None = None) -> go.Figure: ...
     def _reorder_sims(self, new_order: t.Sequence[int]) -> None: ...
     def _wrap_result_with_coupling(self, result_array: t.Any, inputs: tuple[t.Any, ...]) -> StochasticScalar: ...
